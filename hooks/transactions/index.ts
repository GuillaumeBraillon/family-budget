/**
 * @file Barrel export pour les hooks de gestion des transactions
 * @description Point d'entrée centralisé pour tous les hooks liés aux transactions variables.
 */

export { useTransactionForm } from "./useTransactionForm";
export type { FormMode, TransactionType } from "./useTransactionForm";
